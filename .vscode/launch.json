{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Frontend (index.html in public)",
      "url": "http://localhost:3000/create/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
